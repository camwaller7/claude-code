// Shared constant for the current Terms of Service version.
// Lives in its own file (no server-only imports) so it can be
// safely imported by both server code and client components.
export const CURRENT_TERMS_VERSION = 1;
