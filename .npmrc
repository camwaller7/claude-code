# vitest@5 declares a peerOptional on @types/node ^22 while this project pins
# @types/node@20 (matching an earlier baseline). The versions are compatible in
# practice; legacy-peer-deps lets `npm install` proceed (as it does locally and
# is required for Vercel's clean install to succeed) without changing any
# resolved dependency versions.
legacy-peer-deps=true
